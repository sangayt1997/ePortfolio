import styled from "styled-components";

export const AboutContainer = styled.div`
  display: flex;
  min-height: 540px;
  
  .progress {
    height: 8px;
  }
  
  .progress-bar {
     transition: 1s ease;
    transition-delay: 0.5s;
  }
`
